package communications

func UserDetail(id int32) {
	// conn, err := grpc.Dial(fmt.Sprintf("localhost:%v", os.Getenv("USERS_SERVICE")), grpc.WithInsecure())
	// if err != nil {
	// 	panic(err)
	// }
	// client := proto.NewUsersServiceClient(conn)
	// req := &proto.RequestDetail{Id: int32(id)}
	// if res, err := client.Detail(context.Background(), req); err == nil {
	// 	return res.Name
	// }
}
